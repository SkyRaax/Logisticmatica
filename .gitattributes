# Normalize line endings: LF in the repo for text, so Linux CI + Windows agree.
* text=auto eol=lf

# Scripts that are platform-specific
gradlew      text eol=lf
gradlew.bat  text eol=crlf

# Binary assets — never touch
*.jar  binary
*.png  binary
*.ico  binary
